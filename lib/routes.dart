import 'package:poc_app/layout.dart';
import 'package:flutter/widgets.dart';

final Map<String, WidgetBuilder> routes = {
  Layout.routeName: (context) => Layout(),
  // Home.routeName: (context) => Home(),
};
